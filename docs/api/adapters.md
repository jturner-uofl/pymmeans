# `pymmeans.adapters`

Adapter protocol for plugging custom model classes into pymmeans.
`register_adapter` extends pymmeans to recognise new fitted-result
shapes; `LinearmodelsAdapter` / `StatsmodelsAdapter` are the
built-in implementations.

::: pymmeans.adapters
